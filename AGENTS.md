## 本任务内的 Luna 子代理分工

- 主模型保持用户选择的模型，负责理解需求、规划、关键决策、整合与最终审核，不自行切换主模型。对于能独立分工且能节省时间或提高质量的工作，主动使用子代理；简单任务直接完成，不为并行而拆分。
- 创建执行子代理时显式指定模型 `gpt-5.6-luna`，推理强度默认 `medium`。由主模型按独立子任务的实际需要决定并发数量，不设人为固定数量上限，仍受平台可用槽位、额度及实际资源约束；简单任务直接完成，避免重复劳动和文件冲突。子代理不自行继续创建子代理。若模型不可用或工具不支持指定模型，说明情况，由主模型继续，不偷偷改用其他模型。
- 每次委派明确目标、允许修改的文件范围、必要上下文和验收标准；不同执行者不同时修改同一文件或二进制资产。子代理只使用主任务已分配的工作区，不重新领取任务池槽位、不另建顶层任务，不触碰其他工作区。
- 子代理返回简洁的改动、验证结果和未解决问题；主模型检查关键改动并执行必要的集成验证，不机械重复完整扫描和测试，不把子代理声称通过当作已核实结果。所有执行者继续遵守原有项目、安全、验证和提交流程，不自动提交、回滚、覆盖他人修改或降低验收要求。
- 用户要求停止时停止本任务及其子代理。遇到额度不足时不自动重试、不自动换模型或使用额度重置；报告实际状态。控制上下文与重复劳动，不承诺多代理必然更省额度。